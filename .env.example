# Local PostgreSQL (Docker)
DATABASE_URL=postgresql://pokecollection:pokecollection@localhost:5433/pokecollection_dev

# Render PostgreSQL (production)
# DATABASE_URL=postgresql://user:password@host/database
